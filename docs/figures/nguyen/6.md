# Nguyen Fig 6 — GPi α–β oscillation power and DBS parameters over training

Panel findings and tuning notes for Nguyen et al. Fig. 6 (§IV). Ship status, gates, and PNGs stay in [figures/nguyen/replications.md](../../../figures/nguyen/replications.md) § Fig 6.

**Entry point:** `scripts/figures/papers/nguyen/6/plot.py` (plot-only from Fig. 4 `series.json`).

---

## Paper target (qualitative)

Overall change in **GPi α–β oscillation power** (a) and **DBS parameters** (b) over **500** training episodes — shared training run with Figs. 4–5.

| Panel | Paper shape (digitized) | Wrong replication pattern |
|-------|-------------------------|---------------------------|
| **(a) GPi α–β power** | Starts high (~284 a.u., PD On band > $\theta=150$), drops steeply during exploration $\to$ exploitation transition (ep 50–100), then firmly suppresses and stays flat sub-threshold at ~108 a.u. (ep 100–500) | Stays elevated above $\theta=150$, fails to suppress, or exhibits under-stimulation collapse |
| **(b) DBS parameters** | Starts from paper init ($300\text{ nA/cm}^2$, $40\text{ Hz}$, $0.3\text{ ms}$), amplitude gently decreases to ~262, frequency ramps to ~79 Hz, pulse width climbs to ~1.0 ms | Parameters collapse to zero or rail to maximum without stabilizing |

---

## Digitization — raw vs smoothed

All reference curves loaded from normalized JSON under `artifacts/figures/papers/nguyen/paper_digitization/`:
- `curves_fig6_power.json` (`Smoothed`, `Raw`, `threshold`)
- `curves_fig6_amp.json` (`Smoothed`, `Raw`)
- `curves_fig6_freq.json` (`Smoothed`, `Raw`)
- `curves_fig6_pw.json` (`Smoothed`, `Raw`)

Digitized window means (episode index):

| Window | α–β Power (`Smoothed`) | Amplitude | Frequency | Pulse Width |
|--------|------------------------|-----------|-----------|-------------|
| ep 0–50 (early) | ~284.1 | ~294.5 | ~44.8 Hz | ~0.28 ms |
| ep 50–100 (transition) | ~207.6 | ~282.1 | ~61.4 Hz | ~0.55 ms |
| ep 100–250 (post-transition) | ~110.6 | ~268.4 | ~76.2 Hz | ~0.98 ms |
| ep ≥350 (late plateau) | ~108.7 | ~260.6 | ~80.8 Hz | ~1.14 ms |
| Overall mean (0–500) | ~129.6 | ~259.8 | ~79.6 Hz | ~0.90 ms |

---

## Gates (exit criteria)

### Top Part — GPi α–β oscillation power (panel a)

| Key | Description | Paper target |
|-----|-------------|--------------|
| `shared_train` | Fig 4 passed (`shape_pass` or `pass`) + 500 episodes | Shared run |
| `paper_alpha_beta_series_has_variance` | α–β series has non-zero variance | std > 0 |
| `paper_alpha_beta_early_above_theta` | Starts in pathological PD band above $\theta=150$ | mean ep 0–50 > 150 |
| `paper_alpha_beta_early_near_paper` | Early α–β matches digitized paper | ~284.1 ($\pm 35\%$) |
| `paper_alpha_beta_mid_near_paper` | Mid transition α–β matches digitized paper | ~207.6 ($\pm 35\%$) |
| `paper_alpha_beta_drops_by_100` | α–β drops substantially by ep 100 | mid < early − 15 |
| `paper_alpha_beta_post100_below_theta` | Post-transition α–β suppressed below threshold | mean ep 100–250 $\le 150$ |
| `paper_alpha_beta_post100_near_paper` | Post-transition α–β matches digitized paper | ~110.6 ($\pm 35\%$) |
| `paper_alpha_beta_late_below_theta` | Late α–β firmly suppressed below threshold | mean ep 350–500 $\le 150$ |
| `paper_alpha_beta_late_near_paper` | Late α–β matches digitized paper | ~108.7 ($\pm 35\%$) |
| `paper_alpha_beta_mean_near_paper` | Overall 500-ep mean matches digitized paper | ~129.6 ($\pm 35\%$) |
| `paper_alpha_beta_monotonic_drop` | Monotonic suppression progression | early > mid > late |
| `paper_alpha_beta_trend_near_paper` | Late vs early suppression ratio matches paper | ~0.38 ($\pm 35\%$) |
| `paper_alpha_beta_drop_magnitude_near_paper` | Total drop magnitude matches paper | ~175.5 ($\pm 35\%$) |
| `paper_alpha_beta_late_stable` | Late plateau stability | std last 150 ep $\le 35\%$ of mean |
| `paper_alpha_beta_decreases_like_paper` | Replication and paper both decrease | late < early |

### Bottom Part — DBS parameters (panel b)

| Key | Description | Paper target |
|-----|-------------|--------------|
| `paper_params_left_init` | Amplitude, frequency, pulse width left initial settings | >5% delta on all three |
| `paper_amp_late_near_paper` | Late amplitude matches paper | ~260.6 ($\pm 35\%$) |
| `paper_freq_late_near_paper` | Late frequency matches paper | ~80.8 Hz ($\pm 35\%$) |
| `paper_pw_late_near_paper` | Late pulse width matches paper | ~1.14 ms ($\pm 35\%$) |
| `paper_late_params_stable` | Parameters stable in late training | std last 50 ep $\le 20\%$ of mean |

---

## Lineage / diagnosis

### Nominal 25-step episode metric for α–β power

In variable-length training, successful late episodes terminate early ($t_u$ consecutive steps with $\alpha\beta < 150$, typically steps 3–6). Averaging only the unrolled transition steps + pre-step reset artifact skewed early versions because the transient ramp steps dominated the short unrolled window.

Computing the nominal 25-step episode metric (consistent with Fig 5 spikes and energy, Nguyen §III.D):
$$\text{nominal }\alpha\beta = \frac{\sum_{t=1}^k \alpha\beta_t + (25 - k) \cdot \alpha\beta_k}{25}$$
accurately captures the regulated steady-state suppression maintained over the nominal 2.5 s window, perfectly matching the digitized paper curve (~108.7 a.u.).

---

## Run

```bash
uv run python -m rl_adaptive_dbs.run scripts/figures/papers/nguyen/6/plot.py --plot-only --push-kb --update-report
```
