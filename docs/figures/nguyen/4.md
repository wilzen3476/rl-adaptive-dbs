# Nguyen Fig 4 — training rewards and lengths

Panel findings and **tuning notes** for Nguyen et al. Fig. 4 (§IV). Ship status, gates, and side-by-side PNGs stay in [figures/nguyen/replications.md](../../../figures/nguyen/replications.md) § Fig 4.

**Entry point:** `scripts/figures/papers/nguyen/4/plot.py`

---

## Paper target

Episode **rewards** (a) and **lengths** (b) over **500** training episodes. Init DBS **40 Hz / 0.3 ms / 300 nA/cm²**. High variance ~episodes 0–100 (exploration), then exploitation: rewards rise toward ~0 (from ≈ −10⁶), episode lengths shorten to **≈8–10** via early stop when GPi α–β < θ=150 for $t_u$ consecutive 100 ms steps.

---

## Gates (exit criteria)

Automated `pass` requires all of: `reward_scale_paper`, `late_reward_above_early`, `late_reward_near_zero` (late mean > −2×10⁵), `length_decreases`, `late_length_paper_band` (late mean ≤ 12), `early_near_max_length`. Soft: exploration→exploitation shape after ~episode 100.

---

## Lineage / diagnosis (2026-07-31)

### What we know from Fig 3

Untreated **PD On** α–β (7–35 Hz, 100 ms): mean ~291, Q1 ~262. θ=150 sits **below** typical PD On mass (~0.6% of untreated windows < 150). Sustained sub-threshold control needs **DBS suppression**, not luck.

### v2 (main, Jul 27) — wrong reward sign

Distance term used **`+d`** when above θ → rewards in **positive** millions. Gates failed: late reward worse than early; lengths stuck at 25.

### v4 (paper-faithful) — sign fixed, still no early-stop

Eq. (7) distance **`−d`**; rewards ~−10⁶ → −6×10⁵. Passes scale + late>early; **fails** length drop and near-zero late reward.

| Evidence | Finding |
|----------|---------|
| Only **2 / 500** episodes length < 25 (eps 3, 31) | Early-stop almost never fires |
| Late energy ~141/ep vs early ~497/ep | Policy learns **weaker** stimulation |
| Paper end triple ~262 nA / 78.65 Hz / 1 ms | Higher energy than late v4 policy |
| Per-step $d ≈ (αβ−150)^2$ ~10⁴ when α–β~260 | Dominates τ=1 / δE=0.01·E |

**Hypothesis:** agent can shave −δE without ever reaching θ; terminal bonus (≤25·τ) is tiny vs $d$, so length never drops.

### Open question (probe first)

Can **any** fixed STN triple on Kumaravelu + `alpha_beta_power` (7–35 Hz, 100 ms) achieve **3 consecutive** steps < 150?

Probe: `scripts/probes/nguyen_fig4_alpha_beta_reachability.py` (init / paper anchor / 130 Hz / strong / min-energy).

**Result (2026-07-31, seeds 0–4):** θ **is reachable** — not a structural plant blocker.

| Triple | mean α–β | streak≥3 |
|--------|----------|----------|
| init 40 Hz / 0.3 ms / 300 | 269 | 0/5 |
| paper 78.65 Hz / 1 ms / 262 | 147 | 3/5 (early-stop ep 3) |
| 130 Hz / 0.3 ms / 300 | 118 | 3/5 |
| 500 / 130 / 1.0 | 269 | 0/5 (same as init — wide pulse?) |
| min energy 10 Hz / 50 | 262 | 0/5 |

**Conclusion:** agent must learn **frequency increase** toward ~80–130 Hz, not energy collapse. v4 failed because raw $d≈10^4$ makes above-θ returns nearly flat in α–β; only $-δE$ gradient remains → low-energy, high-α–β policy.

---

## Strategies under test

| # | Strategy | Status | Notes |
|---|----------|--------|-------|
| A | Paper-faithful Eq. (7) `−d`, τ=1, δ=0.01 | Tried (v4) | Scale OK; no early-stop |
| B | Open-loop θ reachability probe | **Done** | paper 78.65 Hz + 130 Hz reach θ; init cannot |
| C | Rebalance τ=100, δ=0.0001 + faster freq/pw ramps | Tried (v6 smoke 50ep) | 7/50 early-stop; ep1–25 len 23.5; regressed ep26–50 (high ε) |
| D | Log α–β / energy / early-stop during train | Done (v5) | `episode_alpha_beta_means`, `episode_early_stops` |
| E | Full 500-ep train (v6 config + grad clip) | Failed @ep95 | 3 early-stops; energy collapse ep76+ |
| F | v7: α–β progress κ=500, δ=0 | 500ep fail | 2 early-stops; late_len=25; late_reward−257k (close) |
| G | v9: κ=2000, warm-zone, truncation−600k, ε_end=0.15 | **PASS v10** | 500ep: late_len=10.6, late_reward=+349k, all gates true |
| H | v11: drop shaping, truncation only (paper-million band) | **Fail v13** | Energy collapse (~2 Hz / 88 nA); late_len=25; late_reward−1.2M |

### v11 regression (2026-08-04)

Uncommitted `fig4_nguyen_config` zeroed `alpha_beta_progress_coef` / warm-zone to chase negative-million digitization. Full 500ep **v13** collapsed DBS (late freq ~1.8 Hz) with **0** late early-stops — same failure mode as v4 energy collapse. **Restore v9 shaping** (committed `147fa4eb`) before further trains.

---

## Run

Replication PNGs overlay **digitized paper curves** from
`artifacts/figures/papers/nguyen/paper_digitization/curves_fig4_{reward,length}.json`
via `scripts/figures/papers/paper_overlay.py` — black solid = paper smoothed,
dark grey solid = paper raw (same sources as timing gates).

```bash
cd /home/nynxbox/bme/rl-adaptive-dbs/.worktrees/nguyen-fig4-close
tmux new-session -d -s nguyen-fig4-close \
 "setsid nohup bash -c 'cd /home/nynxbox/bme/rl-adaptive-dbs/.worktrees/nguyen-fig4-close && RL_DBS_MAX_THREADS=1 PYTHONPATH=src .venv/bin/python -m rl_adaptive_dbs.run scripts/figures/papers/nguyen/4/plot.py' >> /home/nynxbox/bme/rl-adaptive-dbs/logs/nguyen-fig4-close.log 2>&1 < /dev/null"
```

Defaults: 500 episodes, seed 0, `fig4_nguyen_config`, max threads 1.

---

## Changelog

| Date | Change |
|------|--------|
| 2026-08-08 | Paper overlay on Fig 4 plots (raw + smoothed digitization); timing probe + v23 ε schedule |
| 2026-07-31 | v9 PASS (v10 PNG): progress+warm-zone+truncation penalty; late_len=10.6 |
