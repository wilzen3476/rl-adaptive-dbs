# Nguyen Fig 4 — training rewards and lengths

Panel findings and **tuning notes** for Nguyen et al. Fig. 4 (§IV). Ship status, gates, and side-by-side PNGs stay in [figures/nguyen/replications.md](../../../figures/nguyen/replications.md) § Fig 4.

**Entry point:** `scripts/figures/papers/nguyen/4/plot.py`

---

## Paper target

Episode **rewards** (a) and **lengths** (b) over **500** training episodes. Init DBS **40 Hz / 0.3 ms / 300 nA/cm²**. High variance ~episodes 0–100 (exploration), then exploitation: rewards rise toward ~0 (from ≈ −10⁶), episode lengths shorten to **≈8–10** via early stop when GPi α–β < θ=150 for $t_u$ consecutive 100 ms steps.

---

## Gates (exit criteria)

Automated `pass` is **first 100 episodes only**. Train horizon for this phase is **100 episodes** (paper figure is 500; extra episodes are not scored). Digitized paper: length 0–50 ≈25, 50–100 median ≈17.8, **80–100 ≈10**; reward 0–50 ≈ −4.5×10⁵, 80–100 ≈ −4.6×10⁴.

| Focus | Required keys |
|-------|----------------|
| Reward how far | `reward_scale_paper` (early \|mean\| large), `reward_by_100_near_zero` (80–100 toward ~0) |
| Reward how fast | `reward_improves_by_100` (80–100 better than 0–50) |
| Length how far | `early_near_max_length` (raw median ~25), `length_early_smoothed_near_horizon` (smoothed 0–50 ≥ 23), `length_by_100_near_paper` (80–100 ~10) |
| Length how fast | `length_mid_glide_like_paper` (drop ep 50–100) |

Logged, not required: late length/reward, post-100 plateaus, `late_reward_above_early`, paper reward mag/ratio/improves, `length_decreases`. Do **not** re-require paper reward mag/ratio — chasing those caused the v11/v13 energy-collapse regression.

---

## Lineage / diagnosis (2026-07-31)

### What we know from Fig 3

Untreated **PD On** α–β (7–35 Hz, 100 ms): mean ~291, Q1 ~262. θ=150 sits **below** typical PD On mass (~0.6% of untreated windows < 150). Sustained sub-threshold control needs **DBS suppression**, not luck.

### v2 (main, Jul 27) — wrong reward sign

Distance term used **`+d`** when above θ → rewards in **positive** millions. Gates failed: late reward worse than early; lengths stuck at 25.

### v4 (paper-faithful) — sign fixed, still no early-stop

Eq. (7) distance **`−d`**; rewards ~−10⁶ → −6×10⁵. Passes scale + late>early; **fails** length drop and near-zero late reward.

| Evidence | Finding |
|----------|---------|
| Only **2 / 500** episodes length < 25 (eps 3, 31) | Early-stop almost never fires |
| Late energy ~141/ep vs early ~497/ep | Policy learns **weaker** stimulation |
| Paper end triple ~262 nA / 78.65 Hz / 1 ms | Higher energy than late v4 policy |
| Per-step $d ≈ (αβ−150)^2$ ~10⁴ when α–β~260 | Dominates τ=1 / δE=0.01·E |

**Hypothesis:** agent can shave −δE without ever reaching θ; terminal bonus (≤25·τ) is tiny vs $d$, so length never drops.

### Open question (resolved)

Can **any** fixed STN triple on Kumaravelu + `alpha_beta_power` (7–35 Hz, 100 ms) achieve **3 consecutive** steps < 150?

**Result (2026-07-31, seeds 0–4):** θ **is reachable** — not a structural plant blocker. (One-off reachability probe; findings kept here.)

| Triple | mean α–β | streak≥3 |
|--------|----------|----------|
| init 40 Hz / 0.3 ms / 300 | 269 | 0/5 |
| paper 78.65 Hz / 1 ms / 262 | 147 | 3/5 (early-stop ep 3) |
| 130 Hz / 0.3 ms / 300 | 118 | 3/5 |
| 500 / 130 / 1.0 | 269 | 0/5 (same as init — wide pulse?) |
| min energy 10 Hz / 50 | 262 | 0/5 |

**Conclusion:** agent must learn **frequency increase** toward ~80–130 Hz, not energy collapse. v4 failed because raw $d≈10^4$ makes above-θ returns nearly flat in α–β; only $-δE$ gradient remains → low-energy, high-α–β policy.

---

## Strategies under test

| # | Strategy | Status | Notes |
|---|----------|--------|-------|
| A | Paper-faithful Eq. (7) `−d`, τ=1, δ=0.01 | Tried (v4) | Scale OK; no early-stop |
| B | Open-loop θ reachability probe | **Done** | paper 78.65 Hz + 130 Hz reach θ; init cannot |
| C | Rebalance τ=100, δ=0.0001 + faster freq/pw ramps | Tried (v6 smoke 50ep) | 7/50 early-stop; ep1–25 len 23.5; regressed ep26–50 (high ε) |
| D | Log α–β / energy / early-stop during train | Done (v5) | `episode_alpha_beta_means`, `episode_early_stops` |
| E | Full 500-ep train (v6 config + grad clip) | Failed @ep95 | 3 early-stops; energy collapse ep76+ |
| F | v7: α–β progress κ=500, δ=0 | 500ep fail | 2 early-stops; late_len=25; late_reward−257k (close) |
| G | v9: κ=2000, warm-zone, truncation−600k, ε_end=0.15 | **PASS v10** | 500ep: late_len=10.6, late_reward=+349k, all gates true |
| H | v11: drop shaping, truncation only (paper-million band) | **Fail v13** | Energy collapse (~2 Hz / 88 nA); late_len=25; late_reward−1.2M |

### v11 regression (2026-08-04)

Uncommitted `fig4_nguyen_config` zeroed `alpha_beta_progress_coef` / warm-zone to chase negative-million digitization. Full 500ep **v13** collapsed DBS (late freq ~1.8 Hz) with **0** late early-stops — same failure mode as v4 energy collapse. **Restore v9 shaping** (committed `147fa4eb`) before further trains.

### v15 anti-farming (2026-08-05, on main)

Probe-driven fix for timeout **progress farming** and Q blow-up: cap per-step α–β progress bonus (`alpha_beta_progress_cap_per_step=10_000`), scale Bellman targets only (`reward_learning_scale=1e-4`; episode logs stay raw for gates), truncation penalty **1e6**. Keeps v23 ε schedule (decay 2200, ε_end=0.05). Probe: `scripts/probes/nguyen_fig4_gate_probe.py`.

---

## Run

Replication PNGs overlay **digitized paper curves** from
`artifacts/figures/papers/nguyen/paper_digitization/curves_fig4_{reward,length}.json`
via `scripts/figures/papers/paper_overlay.py` — black solid = paper smoothed,
dark grey solid = paper raw (same sources as timing gates).

```bash
cd /home/nynxbox/bme/rl-adaptive-dbs/.worktrees/nguyen-fig4-close
tmux new-session -d -s nguyen-fig4-close \
 "setsid nohup bash -c 'cd /home/nynxbox/bme/rl-adaptive-dbs/.worktrees/nguyen-fig4-close && RL_DBS_MAX_THREADS=1 PYTHONPATH=src .venv/bin/python -m rl_adaptive_dbs.run scripts/figures/papers/nguyen/4/plot.py' >> /home/nynxbox/bme/rl-adaptive-dbs/logs/nguyen-fig4-close.log 2>&1 < /dev/null"
```

Defaults: 500 episodes, seed 0, `fig4_nguyen_config`, max threads 1.

---

### v39 timing miss (2026-08-11) — wrong ε schedule on disk

Ship replot **v39** fails only timing shape: `reward_post100_plateau`, `length_mid_glide_like_paper`, `length_post100_plateau`. Digitized paper length: early≈25 → mid(50–100)≈17.8 → post(100–150)≈9.85 flat. Cached `series.json` was trained with **`epsilon_decay_steps=4200`** (ε≈0.53 at ep 100), so length drop starts ~ep 110 and keeps gliding through 100–250 (ptp≈8.25).

### v40 fail (2026-08-11) — decay=2200 late regression

Fresh 500ep with **decay=2200 / tu=2 / trunc=1e6** (`training_reward_length_v40.png`): mid still no glide (smoothed mid≈20.6), post-100 lvl≈18.0 vs paper≈9.85, **late_len≈16.6** (fails `late_length_paper_band`), late reward≈−401k, never reaches smoothed len≤12. Early anneal hard-locks a weak greedy policy; contrast v39 decay=4200 late_len≈8.9. 150-ep shaping probes overstated mid-glide under 2200 — full horizon regresses.

**v41 knobs (paper-silent):** `epsilon_decay_steps=3200` (between 2200 and 4200), `alpha_beta_progress_coef=2500`, `frequency_sensitivity=20` — keep trunc=1e6 / tu=2 / ε_end=0.05.

### v41 fail (2026-08-11) — mid-glide without late floor

Full 500ep (`training_reward_length_v41.png`): smoothed mid≈17.9 (glide gate true) but length **rose** after ε floor (150–250 mean≈19.5); late_len≈**18.7**, late_reward≈**−791k**. Faster anneal + freq_sens=20 is a step back from Report 3 / v22 (late_len≈8.9, late_reward≈+378k).

**v42:** restore `epsilon_decay_steps=4200` and `frequency_sensitivity=15`; keep `prog=2500` so early-stop is worth taking in the high-ε 50–100 window. Hard floor: do not ship a run that loses `late_length_paper_band`.

### v42 fail (2026-08-12) — 4200 + prog=2500 did not recover v22

Full 500ep (`training_reward_length_v42.png`): late_len≈**17.4**, late_reward≈**−518k**, mid 50–100 mean≈22 (no paper glide). Restoring decay=4200 alone does not reproduce v22/v39 (`late_len≈8.9`) once trunc=1e6 / ε_end=0.05 / `reward_learning_scale=1e-4` / prog=2500 are in the loop.

**v43:** restore the v10 PASS recipe — `epsilon_decay_steps=3500`, `epsilon_end=0.15`, `alpha_beta_progress_coef=2000`, `truncation_penalty=600k`, `reward_learning_scale=1.0`, keep tu=2 / freq_sens=15 / mild progress cap. Required gates narrowed to levels + timing (see Gates above).

### v43 fail (2026-08-12) — unscaled Bellman Q blow-up

Full 500ep (`training_reward_length_v43.png`, ~62 min): late_len=**25.0**, late_reward≈**−1.07e6**. Early-stop 34% in ep 0–50 then **0% after ep 100**. Energy 660→82; α–β stuck ~297 (untreated PD). DQN loss 10^9–10^10. Dropping `reward_learning_scale` reintroduced the v15 Q-explosion; v10's unscaled Bellman is not safe on the current trainer.

**v44:** same v10 schedule/shaping as v43, restore `reward_learning_scale=1e-4`.

### v44 fail (2026-08-13) — right direction, not far enough

Full 500ep (`training_reward_length_v44.png`, ~52 min): late_len≈**19.5**, late_reward≈**−550k**. Loss stable (~25–400). Early-stop 34% → 16% mid → **57% late**, but late α–β still ~245 (θ=150) so stops are long (~19 steps, not ~8). Smoothed mid length **rose** (20→22.9) — no 50–100 glide. `ε_end=0.15` leaves 15% random actions forever, which fights a short greedy plateau.

Required 8: only `reward_scale_paper` + `early_near_max_length` true.

**v45:** decay=4200 / `ε_end=0.05` / prog=2000 / trunc=600k / scale=1e-4 (v22 exploit floor, keep Bellman scale).

### v45 (2026-08-13) — late better, first 100 still wrong

Full 500ep (`training_reward_length_v45.png`, ~39 min): late_len≈**14.5**, late_reward≈**−235k**. First 100: smoothed length 0–50 ≈20 / 50–100 ≈22 / 80–100 ≈21 (paper 25 / 18 / **10**); reward 80–100 still ≈−780k (paper ≈−46k). Raw median first 50 is 25, but 34% lucky early-stops pull the smoothed start down and there is no 50–100 glide.

**v46:** required gates = first 100 only. `t_u=3` (paper; fewer accidental early-stops) + v41 schedule (decay=3200, freq_sens=20, prog=2500, trunc=1e6), keep scale=1e-4.

### v46 fail (2026-08-13) — t_u=3 made the first 100 worse

Full 500ep (`training_reward_length_v46.png`): first-100 length **rose** (smoothed 0–50 ≈21.5 → 80–100 ≈24.6 vs paper ~10). Reward 80–100 ≈−1.5e6. `t_u=3` did not pin 0–50 at 25 (still 34% early-stop) and blocked the 50–100 glide.

**v47:** train **100 episodes** only; restore `t_u=2`; keep v41 schedule.

### v47 fail (2026-08-13) — mid-glide, ε still 0.44 at ep 100

100ep in ~9 min (`training_reward_length_v47.png`). First-100: `length_mid_glide_like_paper` **true** (smoothed 50–100 ≈17.9, paper 17.8), `reward_improves_by_100` true. Fail: smoothed 0–50 ≈20 (need ≥23), 80–100 ≈18 (paper ~10), reward 80–100 ≈−791k. Log ε=0.44 at ep 100 (`decay=3200` is sized for ~500ep). Random actions at that rate cannot average length ~10.

**v48:** `epsilon_decay_steps=1900` so ε≈0.12 by ep 80 on a 100-ep train. Same t_u=2 / freq_sens=20 / 100 episodes.

### v48 fail (2026-08-14) — fast linear anneal locked a weak greedy policy

100ep in ~11 min (`training_reward_length_v48.png`). ε hit 0.05 by ~ep 85 as intended, but greedy still timed out: 80–100 raw median **25**, smoothed ~22 (paper ~10). Mid-glide **false** (50–100 ≈22.6 vs paper 17.8). Reward 80–100 ≈−1.15e6. Faster linear decay from step 0 recreates the v40 weak-greedy lock; do not repeat it.

**v49:** paper-silent delayed ε — hold ε=1 for ~50 episodes (`epsilon_decay_delay_steps=1100`), then decay over 800 steps so ε≈0.05 by ~ep 85. Keep t_u=2 / freq_sens=20 / scale=1e-4 / 100 episodes. Goal: v47's 50–100 glide, then drop ε so 80–100 can show greedy length ~10.

### v49 fail (2026-08-14) — delay too long; 80–100 improved, early/mid got worse

100ep in ~10 min (`training_reward_length_v49.png`). ε=1 through ep 50, 0.33 at ep 80, 0.05 at ep 100. Smoothed length 0–50 ≈**19.1** (need ≥23; random lucky early-stops), 50–100 ≈22.3 (mid-glide **false**), 80–100 ≈**16.7** (paper ~10; better than v48's ~22). `reward_improves_by_100` true; 80–100 reward ≈−456k (need > −2e5). Holding ε=1 for the whole first half recreates accidental early-stops; do not repeat delay=1100.

**v50:** delay=500 (~22 ep) then `epsilon_decay_steps=1500` so 0–50 is not all-random, 50–80 can glide, ε floor by ~ep 90.

### v50 fail (2026-08-14) — shorter delay did not recover the early horizon or 80–100 drop

100ep in ~9 min (`training_reward_length_v50.png`). ε=1 at ep 25, 0.69 at ep 50, 0.34 at ep 80, 0.13 at ep 100. Smoothed length stayed flat: 0–50 ≈**18.7**, 50–100 ≈18.7, 80–100 ≈**18.4** (paper 25 / 18 / 10). `reward_improves_by_100` true; 80–100 reward ≈−850k. Mid-window happens to sit near the paper mid level, but there is no 0–50 horizon or 80–100 drop. Hold-then-decay (v49/v50) is the wrong family.

**v51:** restore v47's slow slope (`decay=3200`, `delay=0`) — the only first-100 mid-glide — then dump after ~70 episodes (`epsilon_accelerate_after_steps=1400`, dump=500) so ε≈0.05 by ~ep 90. Do not hold ε=1 and do not anneal from step 0 as fast as v48.

### v51 fail (2026-08-14) — mid-glide back; dump too late for 80–100

100ep (`training_reward_length_v51.png`). Mid-glide **true** (50–100 ≈17.2 vs paper 17.8). Start still ~20 (need ≥23). 80–100 ≈**17.0** (paper ~10). ε=0.46 at ep 80, 0.12 at ep 100 — dump started after the 80–100 window was already underway. Greedy stops still ~14–17 steps.

**v52:** dump earlier (`accelerate_after=1000`, dump=500) so 80–100 is at the ε floor; `frequency_sensitivity=15` to cut lucky first-50 stops.

### v52 fail (2026-08-14) — earlier dump locked timeouts; freq=15 did not fix the start

100ep (`training_reward_length_v52.png`). ε floor by ep 70. Length 0–50 ≈21.1, 50–100 ≈20.4 (glide **false**), 80–100 ≈**19.4**. Start a bit higher than v51; end and mid worse. Do not dump this early.

**v53:** restore `frequency_sensitivity=20`; dump after ~60 ep (`accelerate_after=1200`) over 350 steps so ε floor by ~ep 80 — between v51 (too late) and v52 (too early).

### v53 fail (2026-08-14) — dump timing OK; greedy still ~17 steps

100ep (`training_reward_length_v53.png`). Mid-glide **true** (50–100 ≈17.3). ε=0.11 at ep 80, 0.05 at ep 100. 80–100 still ≈**17.2** (paper ~10); 0–50 ≈20.0. Greedy stops at ~14–17 with α–β ~220 — not a full-horizon 500ep candidate yet (need start ~25 and end ~10).

**v54:** keep v53 ε; `alpha_beta_progress_coef=4000` so the policy is paid more for driving α–β down and can stop earlier. Full 500ep train only after first-100 shape is close (0–50 ≳ 23, 80–100 ≲ 13).

### v54 fail (2026-08-14) — same curve as v53; start is lucky tu=2 stops

100ep (`training_reward_length_v54.png`). Smoothed 0–50 ≈19.95 / 50–100 ≈17.3 / 80–100 ≈17.2. Raw median first 50 is already **25**; 16/50 episodes early-stop (10 of them length ≤10) and pull the moving average down. Progress 4000 was a no-op vs v53.

**v55:** keep v53 ε dump; revert prog=2500; `frequency_sensitivity=10` so a random +freq walk from 40 Hz is less likely to hit suppressing ~80 Hz in the first 50 episodes. Goal: smoothed 0–50 toward 25.

### v55 (2026-08-14) — length start ok; first-episode reward too low

Episode 1 length **25**, reward ≈**−1.40e6**. Paper smoothed start ≈**−0.66e6** / length 24.9. Adding back the timeout penalty: −1.40e6 + 1e6 ≈ **−0.40e6**, in the paper’s start band. Smoothed 0–50 length ≈23.4 (horizon gate true); greedy later timed out (80–100 = 25).

**v56:** same freq_sens=10; `truncation_penalty=0` so a 25-step first episode is Eq. (7) only. Target: episode 1 length 25 and reward near −0.65e6.

### v56 (2026-08-14) — ep1 length 25; reward too high (−0.40e6)

Episode 1: length 25, reward **−3.98×10⁵** vs paper smoothed start **−6.6×10⁵**. Removing the 1e6 penalty overshot.

**v57:** `truncation_penalty=250_000` (paper-silent). Expected ep1: −3.98e5 − 2.5e5 ≈ **−6.5e5**, length still 25.

### v57 (2026-08-14) — ep1 start matches paper; greedy still times out

Episode 1: length **25**, reward **−6.48×10⁵** (paper smoothed start ≈ **−6.61×10⁵**). Smoothed 0–50 length ≈23.5 (horizon gates true). After ε floor (~ep 66) the greedy policy timed out: 80–100 length ≈**24.1** (paper ≈10.1), reward ≈**−8.8×10⁵** (worse than start). `freq_sens=10` kept random walks off ~80 Hz (good start) but starved early-stop examples, so Q never locked onto +freq.

**v58:** keep trunc=250k / freq_sens=10 (do not give up the start). Move the ε dump to ~episode 59 (`accelerate_after=1400`, dump=400) so the 20-ep moving average can turn there. `replay_update_cadence=16` (paper-silent; default 128 gives ~10 SGD steps before the dump on a 100-ep train). Goal: reward starts rising and length starts falling around episode 59.

### v58 (2026-08-14) — faster replay hurt the start; still no glide

Episode 1: length **25**, reward **−6.59×10⁵**. Smoothed 0–50 length ≈**22.6** (horizon gate false; v57 was 23.5). 50–100 and 80–100 stayed ≈**22.6** (paper ≈17.8 / 10.1). Reward 80–100 ≈ −5.2×10⁵ (`reward_improves_by_100` true, still far from ~0). Episode 59 had one length-9 stop, then timeouts.

**v59:** revert `replay_update_cadence=128`; `frequency_sensitivity=12` (between v57’s 10 and v52’s 15); keep dump@1400 / trunc=250k. Next if start holds and greedy still times out: freq_sens=15 with the same dump. Do not go back to cadence=16.

### v59 (2026-08-14) — freq=12 still no glide

Episode 1: length **25**, reward **−6.97×10⁵**. Smoothed 0–50 ≈**22.75** (horizon false). 50–100 ≈23.2 (rose, not a glide). 80–100 ≈**22.35** (paper ≈10). Reward 80–100 ≈ −8.4×10⁵ (worse than start). Greedy still times out.

**v60:** `frequency_sensitivity=15`; keep dump@1400 / replay=128 / trunc=250k. Do not restore cadence=16.

### v60 (2026-08-14) — freq=15 collapsed the start

Episode 1: length **25**, reward **−7.21×10⁵**. Smoothed 0–50 ≈**21.4** (horizon false; v52 freq=15 was 21.1). 50–100 ≈20.5, 80–100 ≈**21.4** (paper ≈10). Reward 80–100 ≈ −6.2×10⁵. Do **not** raise freq further.

**v61:** restore `frequency_sensitivity=10` (v57 start) and `epsilon_end=0.15` (v10 late exploration) so 80–100 can still early-stop without lucky first-50 80 Hz hits. Keep dump@1400 / trunc=250k / replay=128.

### v61 (2026-08-14) — ε_end=0.15 did not create a glide

Episode 1: length **25**, reward **−6.59×10⁵**. Smoothed 0–50 ≈**22.55**. 50–100 ≈22.55, 80–100 ≈**23.15** (rose). Reward 80–100 ≈ −7.7×10⁵. Floor at 0.15 still times out.

**v62:** slower dump (`accelerate_decay_steps=800`) so ε keeps falling through 80–100. Keep freq=10 / ε_end=0.15 / trunc=250k. Next if still flat: ε_end=0.20. Do not raise freq. Do not restore cadence=16.

### v62 (2026-08-14) — slower dump made 80–100 all timeouts

Episode 1: length **25**, reward **−6.59×10⁵**. Smoothed 0–50 ≈**22.55**, 50–100 ≈23.2, 80–100 ≈**25.0**. Reward 80–100 ≈ −9.2×10⁵. Dump=800 is a miss; revert to 400.

**v63:** `epsilon_end=0.20`; dump back to 400. Keep freq=10 / trunc=250k. Do not raise freq. Do not restore cadence=16.

### v63 (2026-08-14) — ε_end=0.20 still no glide

Episode 1: length **25**, reward **−6.59×10⁵**. Smoothed 0–50 ≈**22.75**, 50–100 ≈23.45, 80–100 ≈**24.1**. Reward 80–100 ≈ −8.3×10⁵. ε-floor family (0.05 / 0.15 / 0.20) is exhausted under freq=10.

**v64:** `pulse_width_sensitivity=0.2` (paper-silent; init 0.3 ms → paper ~1 ms in fewer +pw steps) without raising freq. Keep dump=400 / ε_end=0.20 / trunc=250k. Do not restore cadence=16. Do not retry dump=800.

### v64 (2026-08-15) — pw_sens=0.2 left 80–100 all timeouts

Episode 1: length **25**, reward **−6.56×10⁵**. Smoothed 0–50 ≈**22.25**, 50–100 ≈24.05, 80–100 ≈**25.0**. Reward 80–100 ≈ −9.3×10⁵. Start held in the 22–23 band (not a v60-style collapse). Wider pulse steps did not produce a greedy glide.

**v65:** `pulse_width_sensitivity=0.3`. Keep freq=10 / ε_end=0.20 / dump=400 / trunc=250k. Do not raise freq. Do not restore cadence=16. Do not retry dump=800. If start collapses, revert pw and escalate.

### v65 (2026-08-15) — pw_sens=0.3 still 80–100 all timeouts

Episode 1: length **25**, reward **−6.57×10⁵**. Smoothed 0–50 ≈**22.3**, 50–100 =**25.0**, 80–100 =**25.0**. Reward 80–100 ≈ −9.4×10⁵. Start held (not a v60 collapse). Pulse-width family (0.1 / 0.2 / 0.3) is exhausted under freq=10.

**Blocked (probe budget).** Open-loop: paper 78.65 Hz / 1 ms reaches θ; init 40 Hz does not. Constant `frequency_sensitivity=10` keeps 0–50 near horizon but starves 80 Hz early-stop examples, so greedy times out (α–β ~300–340). Constant 15 collapsed the start. Do not raise freq globally. Do not restore cadence=16. Do not retry dump=800. Do not raise pw further.

**v66 (scheduled freq_sens):** `frequency_sensitivity_explore=20` linearly vs ε (20 at ε=1 → 10 at ε_end=0.20). High-ε random walks see faster +freq so replay gets 80 Hz early-stop transitions; greedy at the ε floor keeps exploit Hz/step=10 so ep1 start is not collapsed by lucky 80 Hz hits. Keep dump=1400/400 / trunc=250k / pw=0.3. If start collapses (0–50 < 22), lower explore to 15. If 80–100 still timeouts, raise explore to 25 (not constant freq).

Pick one (if v66 fails — wait):

1. ~~**Scheduled freq_sens**~~ (v66 — in flight).
2. **500-ep with v10-like freq=20** after locking the ep1 start (trunc=250k). v10 already reached late_len≈10.6 on 500 ep; the first-100 80–100≈10 gate may be the wrong horizon at freq=10.
3. **Park the 80–100 length~10 gate** and keep the ep1 start match until a later 500-ep pass.

---

## Changelog

| Date | Change |
|------|--------|
| 2026-08-15 | v66: scheduled freq_sens (explore=20 vs ε, exploit=10 at floor) |
| 2026-08-15 | v65 FAIL: pw_sens=0.3, 0–50 ≈22.3, 80–100 =25; pw family exhausted, probe budget |
| 2026-08-15 | v64 FAIL: pw_sens=0.2, 0–50 ≈22.3, 80–100 =25 |
| 2026-08-14 | v63 FAIL: ε_end=0.20, 80–100 ≈24.1 |
| 2026-08-14 | v62 FAIL: dump=800, 80–100 len=25 all timeouts |
| 2026-08-14 | v61 FAIL: ε_end=0.15, 0–50 ≈22.6, 80–100 ≈23.2 |
| 2026-08-14 | v60 FAIL: freq=15 start 21.4, 80–100 ≈21.4; do not raise freq |
| 2026-08-14 | v59 FAIL: freq=12 start 22.75, 80–100 ≈22.4, greedy timeouts |
| 2026-08-14 | v58 FAIL: cadence=16 start 22.6, no glide; one ES at ep 59 |
| 2026-08-14 | v57: ep1 start matches paper (−6.48e5 / len 25); 80–100 still timeouts |
| 2026-08-14 | v56: truncation=0 so ep1 timeout reward matches paper ~−0.65e6 |
| 2026-08-14 | v55: freq_sens=10 to cut lucky first-50 stops (start toward 25) |
| 2026-08-14 | v54: keep v53 ε, progress 4000; 500ep only after first-100 shape |
| 2026-08-14 | v53: freq=20, dump after=1200 / 350 steps |
| 2026-08-14 | v52: earlier dump (after=1000) + freq_sens=15 |
| 2026-08-14 | v51: v47 slope (decay=3200) then dump after 1400 steps |
| 2026-08-14 | v50 FAIL flat ~18.5 length; hold-then-decay family stopped |
| 2026-08-14 | v49 FAIL delay=1100; v50 delay=500 / decay=1500 |
| 2026-08-14 | v48 FAIL greedy timeouts at ε floor; v49 delayed ε (hold 1100 then decay 800) |
| 2026-08-13 | v47 FAIL ε=0.44 at ep 100; v48 decay=1900 for 100-ep horizon |
| 2026-08-13 | Train horizon 100 ep; v46 FAIL first-100 rose; v47 t_u=2 |
| 2026-08-13 | First-100 gates only; v46 t_u=3 + v41 mid-glide schedule |
| 2026-08-13 | v45 late_len≈14.5 but first-100 length stayed ~22 (paper 80–100 ≈10) |
| 2026-08-13 | v44 FAIL late_len≈19.5 (right direction); v45 decay=4200 / ε_end=0.05 |
| 2026-08-12 | v43 FAIL late_len=25 Q-blow-up; v44 restore `reward_learning_scale=1e-4` |
| 2026-08-12 | Required gates = levels + timing only; v43 restore v10 recipe (decay=3500, ε_end=0.15, trunc=600k) |
| 2026-08-12 | v42 FAIL late_len≈17.4; 4200+prog=2500 did not recover v22 |
| 2026-08-12 | v41 FAIL late_len≈18.7; v42 restore decay=4200 + freq_sens=15, keep prog=2500 |
| 2026-08-11 | v40 FAIL decay=2200 late_len≈16.6; v41 → decay=3200 + prog=2500 + freq_sens=20 |
| 2026-08-11 | v39 timing fail root-cause: series used decay=4200; tried fresh 2200 train |
| 2026-08-11 | Demote reward dig mag/ratio + `late_reward_near_zero` to diagnostic (positive shaping vs paper negative-million band) |
| 2026-08-08 | Paper overlay on Fig 4 plots (raw + smoothed digitization); timing probe + v23 ε schedule |
| 2026-07-31 | v9 PASS (v10 PNG): progress+warm-zone+truncation penalty; late_len=10.6 |
