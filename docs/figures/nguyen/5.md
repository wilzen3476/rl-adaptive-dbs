# Nguyen Fig 5 — CBGT spikes + DBS energy (lab notebook)

Panel script: `scripts/figures/papers/nguyen/5/plot.py`. Ship tracker: `figures/nguyen/replications.md`.

## Qualitative target (paper)

- **(a) Spikes:** low and **flat** over training (~800/ep digitized), not a large early peak then decay.
- **(b) Energy:** **ramps up** around episodes **60–70**, then stays high late (digitized Smoothed mid ep 55–75 ≈ 1.27× early; late ≫ early).

## Spike logging (replication convention)

Per-episode spike totals for Fig 5 sum **only `env.step()` transitions** (same steps as `episode_lengths`); the initial `reset()` integrate is excluded.

Each step logs **`cbgt_spike_count` = sum of the binary spike observation matrix** (Nguyen Eq. (4) spike presence per neuron/time bin), not raw plant spike-event counts across GPi+TH+cortex.

## Digitization

| Panel | JSON | Notes |
|-------|------|--------|
| Spikes | `curves_fig5_spikes.json` | Single **Spike Count** series (no Raw/Smoothed split) |
| Energy | `curves_fig5_energy.json` | **Smoothed** + **Raw**; gates use Smoothed |

## Iteration log

| Version | Change | Result |
|---------|--------|--------|
| v7–v11 | Plot from v70/v100 `series.json`; old plant spike sum + reset in totals | Fail — early spikes ~2× paper; energy falls vs paper ramp |
| 2026-08-21 | Eq. (4) obs-sum spikes; step-only totals; mid-energy ramp gates; relog probe | v8 relog — heuristic pass, digitization fail |
| 2026-08-21 | **Root cause energy:** late pw ~0.45 ms vs paper ~1 ms | v103: `pulse_width_min=0.5` |
| 2026-08-21 | v103 train + v9 plot | Fig 4 length **full pass**; spike early dig **pass**; energy still ~465 mean |

## v103 / v9 (pw_min=0.5, v98 recipe)

- **Fig 4:** length `pass=True` (ptp 3.8); reward `pass=False` (rew_ptp 75491 > 50k cap).
- **Fig 5:** `paper_spike_early_near_paper` **pass** (840 vs 810). Mean spike 432 vs 815; energy mean 465 vs 2051. pw late min 0.5 (floor OK) but mean 0.72 ms vs paper ~1 ms; late_len≈6.6 shortens per-episode energy.
- **In flight:** v104 `lr=2.485e-4` for combined Fig 4 pass. v105 candidate: `pulse_width_min=0.8` if energy still low after v104 length holds.

**v109 (2026-08-21):** v98 + `pulse_width_min=0.8`, `pulse_width_sensitivity=0.4`, `frequency_sensitivity_early_episodes=100` — target flat spikes + energy ramp through ep100. Fresh 500ep `nguyen-fig4-v109`.

- **Fig 5:** `paper_energy_mid_ramp_near_paper` **PASS** (mid 1562 vs early 1807; paper ratio). spike_early **FAIL** (1228 vs 810); spike_trend fail; energy_trend fail (late 705 vs early 1807). Means energy **896** vs paper 2051 (up from 465).
- **Fig 4:** `shape_pass=False` — ep80–100 len≈21.9 (no glide); pw early ~1.5 ms too hot for spike early + shape.
- **v110:** v103 pw floor (`pw_min=0.5`, `pw_sens=0.35`) + `freq_sens_early_ep=100` only — keep spike early + try mid-ramp without 0.8 pw floor.

## v110 (pw 0.5 + pw_sens 0.35 + freq_early 100)

- Fig 5: mid-ramp **pass** again; spike_early **1232** (fail); ep80 len **24.2** — freq_early=100 blocks length glide (same as v109).
- Fig 4: `shape_pass=False`, len_80–100≈22.9, rew_ptp **627k**.

## v111 (v101 lr + pw 0.5)

- Fig 4: reward **shape pass**; fail `length_by_100` (smoothed 80–100 **15.1** vs ~10) + `reward_post100_plateau` (rew_ptp **135k**). len glide mid OK but 2.48e-4 cluster matches v105/v106 len_80–100≈15.1.
- Fig 5 v12: spike_early **1232** fail; mid-ramp **fail**; mean energy **684**.

## v112 (v103 replica, lr 2.5e-4 + pw 0.5)

- Fig 4: **NO-OP vs v103** — length **full pass** (ptp 3.8, len_80–100≈12.7); reward `reward_post100_plateau` fail (rew_ptp **75491**).
- Fig 5 v13: spike_early **1232** fail; energy mean **648**; all digitization fail.

## v113 (lr 2.487e-4)

- **FAIL cluster** — back in 2.48e-4 shape break: len_80–100≈**15.05**, rew_ptp **127k**, `shape_pass=False`. **Do not retry lr 2.487e-4.**

## v114 (v112 + short_stop 0.15)

- **CLOSED** — worse than v100: len ptp **5.9**, rew_ptp **122k**, `shape_pass=False`. Do not retry short-stop ≤0.15 on v112 base.

## v115 (v101 replicate, lr 2.48e-4 + pw 0.5)

- **Did not reproduce v101** — rew_ptp **135k** (v101 was 51k); len_80–100≈**15.1** (2.48e-4 cluster). len ptp **4.3** ✓ post100 only.
- Fig 5 v16: same spike_early ~1232 fail pattern.

## v116 (v98 fidelity, pw_min 0.05)

- **Fig 4:** reward **full pass** (rew_ptp **49329**); length fail only `length_post100_plateau` (ptp **4.80**). `checkpoint_v116.pt` archived.
- **Fig 5 v17:** spike_early **1245** (fail); energy_mid **769** (no mid_ramp); `shared_train=false`.

## v117 (pw_min 0.45 — CLOSED)

- **Fig 4:** `shape_pass=False`; len_80–100≈**13.85**; len ptp **5.15**; rew_ptp **166k**. Bridge hypothesis failed.
- **Fig 5 v18:** spike_early **1232**; `shared_train=false`.

## v118 (tu=50 — CLOSED)

- Late collapse: late_len_350_500≈16.7, timeout 39%; rew_ptp **82330**. Same tu=50 failure mode as v77 on different base.
- **Fig 5 v19:** spike_mean **799** (v118 series); still `shared_train=false`.

## v119 (LENGTH FULL — nearest full pass)

- **Length full pass** (ptp **4.40**); reward fail only `reward_post100_plateau` (`rew_ptp` **50233**, miss cap by **233**).
- **Fig 5 v20:** spike_mean 651; `shared_train=false`.

## v120 (in flight)

v119 recipe + **`--short-stop-replay-weight 0.24`** — target `rew_ptp` ≤50k while keeping length full. `nguyen-fig4-v120`.
