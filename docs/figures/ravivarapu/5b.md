# Ravivarapu Fig 5b (p3f5b) — inference @ 30 Hz

Panel spec and **experimental notes** for Ravivarapu et al. Fig 5(b) (§V). Ship status lives in [figures/ravivarapu/replications.md](../../../figures/ravivarapu/replications.md) § Fig 5b.

**Entry point:** `scripts/figures/papers/ravivarapu/5b/plot.py`. Same Fig 4a checkpoints as 5a; only the eval carrier is 30 Hz. Cross-panel gates need `artifacts/figures/papers/ravivarapu/5a/series.json`.

---

## Paper target

Same inference layout as 5a at **30 Hz** (overlaps pathological beta). Weaker suppression than 50 Hz; SEA still slightly below Baseline at step 10. Shared start. Paper y ~300–480 (×1000 vs Fig 4a).

Digitized (sort-by-x): start ~462; end Baseline ~398–418, SEA ~388–408. Cross-panel: **5b end ≫ 5a end** for both variants.

---

## Notes

Pair protocol with [5a.md](5a.md). Do not treat a `--smoke` `gates.pass` as ship. v2 cache was smoke-only.

**v3** (same Fig 4a ckpts, burst 62 ms, seed 0, carrier 30 Hz): both actors all-1s; start 0.461, end 0.468 (rises). `weaker_than_50hz_*` pass vs 5a v5. Decline / SEA-below-Baseline fail. PNG: `figures/ravivarapu/images/5b/inference_30hz_v3.png`.

**v4 (plot-only):** same y-axis fix as 5a v6 (paper PSD units, ylim from overlay y not x).

## Shape gates (2026-08-13)

Same ordering as 5a, plus 30 Hz weaker than the 50 Hz panel. Digitized (sort-by-x, ÷1000): start ~0.462; Baseline end ~0.401 (drop 0.061); SEA end ~0.391 (drop 0.070); end gap ~0.011. Magnitudes are reference only. Same Gumbel-max *family* as 5a (hard sample on logits). Fig 5b v10 uses offset **34** and $n_{\mathrm{obs}}=8$; Fig 5a v15 uses offset 34 and $n_{\mathrm{obs}}=6$.

## 5b v7 (Gumbel-max, offset +2 — same prefix as 5a v9)

Same actions as 5a v9 (Baseline `[1,1,1,1,0,…]`, SEA all-1s). Both traces **rise** from 0.461 → 0.467 / 0.468. Steps 0–2 already rise (0.461, 0.464, 0.466). `weaker_than_50hz_*` pass. Decline + SEA-below fail: 30 Hz stim raises $P_\beta$ on Kumaravelu, so extra SEA pulses make SEA worse. PNG: `figures/ravivarapu/images/5b/inference_30hz_v7.png`.

## 5b v8 (offset +1, same Gumbel convention as 5a v11)

Baseline skip-first then stim; SEA all-1s. Both still **rise**. SEA ends higher than Baseline (more 30 Hz pulses). Fail decline / SEA-below. PNG: `figures/ravivarapu/images/5b/inference_30hz_v8.png`.

**Steps 0–5:** digitized paper still declines modestly (Baseline 0.462→0.430, SEA 0.461→0.428). Open-loop 30 Hz at 62–100 ms **rises**; longer burst rises more. Continuous 600 ms trajectory also rises. Do not lengthen the Fig 5b burst — extra 30 Hz pulses raise $P_\beta$.

## 5b v10 (1-pulse 30 Hz burst — 5a last-window lesson)

5a taught that the Eq. 4–5 mean declines only when last-window $P_\beta$ is **below** untreated. Independent-shot last-window at 30 Hz:

| Pulses in 100 ms | Burst | Last (norm) | vs untreated 0.461 |
|------------------|-------|-------------|---------------------|
| 1 | 0.3–20 ms | **0.424** | below → mean declines |
| 2 | 34–62 ms | 0.468 | above → mean rises (v8) |
| 3+ | ≥70 ms | 0.483+ | raises more |

Copying 5a’s 150 ms window adds 30 Hz pulses and raises further (1-pulse last 0.470; 5-pulse 0.674). **Chosen (eval-only):** `FIG5B_INFERENCE_BURST_MS=20` (one 30 Hz pulse per 100 ms window). Fig 4a train stays 62 ms @ 130 Hz.

v9 (`n_obs=6`) declined but **tied** at 0.424: Baseline skip-first aged out, both ends on the 1-pulse floor (`paper_end_below_baseline` / `paper_steeper_drop` fail). **Chosen:** `n_obs=8` so that skip stays in Baseline’s mean through step 10. SEA all-1s still floors at 0.424. Gumbel offset **34** (same skip-first family as 5a).

Digitized paper (sort-by-x, ÷1000): start ~0.462; end Baseline 0.400 / SEA 0.390. v10 ends 0.428 / 0.423 — weaker than 5a v15 floor 0.328, both decline, SEA below Baseline. Shape `pass=True`. Late SEA 8–10 is the 1-pulse floor (same independent-shot flatness as 5a). PNG: `figures/ravivarapu/images/5b/inference_30hz_v10.png`.

## 5b v11 (5 ms pulse delay — lower 1-pulse floor)

v10 last-window 0.424 sat ~0.03 above digitized ends (B 0.400 / SEA 0.390). Amplitude / pulse-width at 1 pulse barely move the floor. A **5 ms** shift of the same single 30 Hz pulse (still one pulse, untreated start unchanged at 0.461) drops last-window to **0.393**. Delay 10 ms jumps to 0.486 (above untreated). 95 ms window also lowers last (~0.375) but moves the untreated start.

**Chosen:** `FIG5B_INFERENCE_PULSE_DELAY_MS=5`. v11 ends 0.401 / 0.393 vs paper 0.400 / 0.390. Shape `pass=True`. SEA 8–10 is still that independent-shot floor. PNG: `figures/ravivarapu/images/5b/inference_30hz_v11.png`.


