# Mehregan Fig 4a — training beta power vs step

Panel spec and **experimental tuning notes** for Mehregan et al. Fig 4a (§IV.A.1). Ship status, replication image, and run commands remain in [figures/mehregan/replications.md](../../../figures/mehregan/replications.md) § Fig 4a.

**Entry point:** `scripts/figures/papers/mehregan/4a/plot.py`

---

## Paper target

Per-step GPi beta-band power during DDPG training on the **45 Hz** fixed-mean pattern alphabet: **300** environment steps (10 episodes × 30 steps). Y-axis **PSD(x10³)** = raw $P_\beta / 1000$. Digitized paper: early (steps ≤130) mean ~0.49, late (≥150) ~0.38, drop ~0.11; mid [120,150] ~0.445 vs early [0,100] ~0.49 (modest mid fade over ~30 steps, not a vertical cliff).

---

## Previous lock (seed 0, τ 3→1.0)

Historical ship before the digitization revisit. Kept for lineage; do not replot as current.

| Knob | Value |
|------|--------|
| Seed | `0` |
| Mean rate | 45 Hz `fixed_mean_pattern` |
| Observation | `state_length=1` (scalar $P_\beta$ per step) |
| Exploration | softmax, τ **3→1.0** (linear anneal; paper-silent schedule) |
| Critic | `critic_action_input=one_hot` |
| Init bias | `init_bias_scale=0.5` |
| Entropy | `entropy_coeff=0.01` |
| Plant | Python, `plant.dt_ms=0.02` |
| Locked train cache | `artifacts/figures/papers/mehregan/4a/series_v18.json` |
| Ship PNG | `figures/mehregan/images/4a/training_beta_v29.png` (cosmetic replot of locked train; gates evaluated on `series_v18.json`) |

**Status:** Digitization revisit in progress (Report 3). Locked **v18** (`series_v18.json`, τ **3→1.0**) passed the old gates but collapsed onto pattern 0 from episode 5 (late β≈0.30 vs digitized paper ≈0.38). Default schedule is now τ **3→1.4** so late softmax still mixes; Fig 4b is paired to the same live `series.json`. Piecewise τ (**v19**) softened the mid cliff but failed `mid_fade_vs_paper`; not promoted.

---

## Digitization revisit (2026-08-13)

Fig 4b episode-mean PSD is the same training run averaged per episode. Matching 4b's **late floor** (digitized ~0.37, still above reward threshold $β_t=0.35$) is the coarse target; 4a's 300-step fade is checked after.

| Diagnosis | Evidence |
|-----------|----------|
| Late cliff = pattern-0 collapse | v4/v18: episodes 0–4 mixed (~18–24 unique actions, β≈0.40–0.44); episodes 5–9 are ≥90% action 0, β≈0.30 |
| Paper late is *not* that floor | Digitized 4b PSD: ep0–1 ~0.50, drop at ep3→4 (~0.47→0.385), then plateau ~0.37 |
| Reward goes positive because late β < $β_t$ | Eq. (8) linear branch when $s < 0.35$; paper late ~0.37 stays quadratic-small / near 0 from below |

**Next train:** `--temperature-end 1.4` (plot.py default). If late β stays on the 0.30 floor, raise τ_end (1.6) or entropy before touching plant scale.

```bash
uv run python -m rl_adaptive_dbs.run scripts/figures/papers/mehregan/4a/plot.py --push-kb --update-report
uv run python -m rl_adaptive_dbs.run scripts/figures/papers/mehregan/4b/plot.py --push-kb --update-report
```

---

## References

- [environment.md](../../environment.md) — RL step, biomarker, reward
- [controllers/ddpg/replication.md](../../controllers/ddpg/replication.md) — DDPG defaults
- [replication-fidelity.md](../../development/replication-fidelity.md) — verified vs open gaps
