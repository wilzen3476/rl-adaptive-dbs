# Mehregan Fig 4a — training beta power vs step

Panel spec and **experimental tuning notes** for Mehregan et al. Fig 4a (§IV.A.1). Ship status, replication image, and run commands remain in [figures/mehregan/replications.md](../../../figures/mehregan/replications.md) § Fig 4a.

**Entry point:** `scripts/figures/papers/mehregan/4a/plot.py`

---

## Paper target

Per-step GPi beta-band power during DDPG training on the **45 Hz** fixed-mean pattern alphabet: **300** environment steps (10 episodes × 30 steps). Y-axis **PSD(x10³)** = raw $P_\beta / 1000$. Qualitative shape: noisy early (~0.43–0.57), sharp drop around steps **130–150**, lower late plateau (~0.35–0.45).

---

## Locked replication (seed 0, v4)

| Knob | Value |
|------|--------|
| Seed | `0` |
| Mean rate | 45 Hz `fixed_mean_pattern` |
| Observation | `state_length=1` (scalar $P_\beta$ per step) |
| Exploration | softmax, τ 3→1 |
| Critic | `critic_action_input=one_hot` |
| Init bias | `init_bias_scale=0.5` |
| Plant | Python, `plant.dt_ms=0.02` |
| Assets | `artifacts/figures/papers/mehregan/4a/series_v4.json`, `checkpoint.pt` |
| PNG | `figures/mehregan/images/4a/training_beta_v4.png` |

**Status:** Needs work — v4 is still the best locked trace (drop ~130–150, trend down) but late mean sits **just below** the automated late gate (0.299 vs 0.30 floor); panel not closed.

---

## Paper-faithful replication stack (2026-07-31)

Mehregan §IV.A.1 fixes the **schedule** (10×30 steps, 45 Hz init, LRs, buffer) and **discrete patterns at fixed mean rate**; it does **not** specify online exploration or irregular-pattern construction. Our documented conventions:

| Layer | Choice | Notes |
|-------|--------|--------|
| Alphabet | `FixedMeanPatternAlphabet` — 41 patterns, pattern 0 regular, 1–40 ±⅓ ISI jitter | [environment.md](../../environment.md) §4.2 |
| Plant | Python Kumaravelu, `plant.dt_ms=0.02` | §IV.A.1 |
| Observation | `state_length=1` | Paper Fig 4a scalar $P_\beta$ |
| **Alg. 1 interaction (deploy)** | greedy softmax + argmax, critic on **logits** | `fig4a_ddpg_config(exploration_mode="greedy", critic_action_input="logits")` |
| **Fig 4a training curve (panel)** | softmax τ **3→1**, `critic_action_input=one_hot`, `init_bias_scale=0.5` | **v4** — labeled deviation; paper silent on training exploration |

**Greedy baseline (done):** `--exploration greedy --critic-action-input logits` collapses to pattern 0 immediately (300/300 action 0). High rewards; **wrong Fig 4a shape** (episode plateaus, no cliff). Artifacts: `series_greedy_baseline.json`, `training_beta_greedy_baseline.png`. Doctrinal Algorithm 1 floor — not the panel profile.

**Not for Fig 4a panel:** tiered jitter alphabet, gradual_c-style delayed anneal / high entropy (worktree diagnostics only).

**Closing the panel:** iterate from **v4 defaults only**, one paper-silent knob at a time (e.g. `--temperature-end` 1.05–1.1) until `late_band` passes without losing `early_band` or `drop_timing_soft`.

```bash
uv run python scripts/figures/papers/mehregan/4a/plot.py
uv run python scripts/figures/papers/mehregan/4a/plot.py --plot-only
```

Long run (~30–60 min). Prefer tmux:

```bash
tmux new-session -d -s fig4a-train \
 "setsid nohup uv run python scripts/figures/papers/mehregan/4a/plot.py >> logs/fig4a-train.log 2>&1 < /dev/null"
```

---

## skip_regular training (Fig 5a path — not Fig 4a)

At 45 Hz, pattern 0 (regular periodic) is the global open-loop optimum. A **41-pattern** Fig 4a agent correctly collapses to it; Mehregan Fig 5a requires trained **above** periodic 45 Hz, which needs excluding pattern 0 from the trained action space.

An experimental worktree added `--skip-regular` directly on the Fig 4a plot script (separate `training_beta_skip_regular_vN.png` / `checkpoint_fig4a_skip_regular.pt`). That path was **not merged**. Production workflow:

- Train skip_regular actor: `scripts/figures/papers/mehregan/5a/plot.py --train`
- Checkpoint: `artifacts/figures/papers/mehregan/4a/checkpoint_skip_regular_02s.pt`
- Fig 4a panel script stays on the **full 41-pattern** space for the training curve; Fig 5a/6a eval use skip_regular checkpoints.

Do not overwrite locked v4 full-alphabet assets when experimenting with skip_regular.

---

## Entropy coefficient (policy-collapse tuning)

A separate experiment exposed `--entropy-coeff` on the Fig 4a plot script so training could slow mid-run $P_\beta$ cliff / policy collapse without changing locked v4 defaults. Core DDPG already supports `entropy_coeff` in `controllers/ddpg/config.py` (`fig4a_ddpg_config` uses `0.01` for softmax, `0.0` for greedy).

**Not merged into the panel script.** To retry manually, pass `entropy_coeff` through `fig4a_ddpg_config(...)` in a local fork or probe — raise slightly (e.g. `0.02–0.05`) if the greedy lock happens too early; lower if exploration stays too noisy late.

---

## Gradual / alternate training curves

`figures/mehregan/images/4a/training_beta_gradual_v1.png` (worktree artifact) explored softer temperature / entropy annealing profiles. Treat as diagnostic only until a profile passes the same qualitative gates as v4 and is promoted via `scripts/figures/papers/promote.py`.

---

## Related diagnostics

| Script | Purpose |
|--------|---------|
| `scripts/probes/fig4a_three_trace_diagnostic.py` | Three-trace softmax / greedy / critic-encoding comparison |
| `scripts/figures/papers/mehregan/4b/plot.py` | Paired reward + episode-mean PSD (reads Fig 4a `series_v4.json`) |

---

## References

- [environment.md](../../environment.md) — RL step, biomarker, reward
- [controllers/ddpg/replication.md](../../controllers/ddpg/replication.md) — DDPG defaults
- [replication-fidelity.md](../../development/replication-fidelity.md) — verified vs open gaps
